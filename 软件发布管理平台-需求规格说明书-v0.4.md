# 软件发布管理平台——软件需求规格说明书

| 项目 | 内容 |
|---|---|
| 文档版本 | v0.4（需求确认稿） |
| 编制日期 | 2026-07-20 |
| 当前运行平台 | Windows |
| 后续兼容目标 | Linux |
| 管理平台后端 | .NET 8 Web API、EF Core、SQLite |
| 管理平台前端 | Vue 3 脚手架 |
| 文档状态 | 需求确认中（已纳入第一轮反馈） |

## 1. 文档目的

本文档定义“软件发布管理平台”的业务范围、功能需求、业务规则、数据要求、接口边界、非功能需求及验收标准，作为后续系统设计、开发、测试和验收依据。

平台用于从 SVN 获取指定项目源码，依次构建 .NET 8 Web API 和两个 Vue 3 前端，将前端产物组装到后台发布目录，管理配置文件，启动或停止已发布程序，并提供日志、历史、压缩包下载等能力。

## 2. 项目背景与目标

### 2.1 源项目标准目录

```text
项目根目录/
└─ 05-trunk/
   ├─ webapi/
   │  └─ iHR.Hosting/
   │     ├─ iHR.Hosting.csproj
   │     └─ appsettings.json
   └─ webui/
      ├─ admin/
      └─ terminal/
```

后台默认发布目录：

```text
05-trunk/webapi/iHR.Hosting/bin/Release/net8.0/publish/
```

最终发布包目录结构至少包含：

```text
publish/
├─ iHR.Hosting.dll
├─ appsettings.json
├─ release-info.json
└─ wwwroot/
   ├─ admin/
   └─ terminal/
```

### 2.2 建设目标

1. 将 SVN 拉取、后端发布、前端构建、产物组装、配置应用、程序运行和打包下载集中到一个 Web 平台。
2. 发布过程可视、可追踪、可重复执行，失败时能够定位到具体阶段。
3. 防止重复点击、并发发布、敏感凭据泄露或错误目录操作造成的风险。
4. 第一阶段可靠运行于 Windows，同时隔离操作系统相关实现，为后续 Linux 适配保留能力。

## 3. 系统范围

### 3.1 本期范围

- 用户登录及基础权限控制。
- 发布项目配置与环境配置。
- SVN 账号、密码和仓库地址配置。
- SVN 首次检出、后续更新及指定修订号发布。
- .NET 8 后端构建发布。
- admin、terminal 两个 Vue 3 项目构建及产物组装。
- `appsettings.json` 在线查看、修改、校验、备份和发布时复用。
- 一键发布、重新发布、停止、启动和重启。
- 实时发布日志、运行日志、操作日志和发布历史。
- 发布产物 ZIP 压缩与下载。
- 发布状态、进程状态和基础健康检查。
- 发布失败保护及最近成功版本回滚。

### 3.2 暂不纳入本期

- Git、Mercurial 等其他源码管理系统。
- Docker、Kubernetes、远程服务器集群发布。
- IIS、Nginx 的自动配置及证书管理。
- 数据库脚本自动执行和数据库备份恢复。
- 多节点负载均衡、灰度发布、审批流。
- SVN 分支合并、冲突在线解决、代码评审。
- 前端源码或后台源码的在线编辑。

上述能力可作为后续版本扩展，但不得阻碍本期 Linux 兼容设计。

## 4. 用户角色与权限

### 4.1 角色

| 角色 | 权限 |
|---|---|
| 系统管理员 | 用户管理、全局设置、项目增删改、凭据管理、发布、运行控制、配置修改、下载、查看全部日志 |
| 发布操作员 | 查看获授权项目、执行发布、启动/停止/重启、下载产物、查看日志；默认不能查看或修改 SVN 密码 |
| 只读用户 | 查看项目状态、发布历史及非敏感日志；不能执行任何变更操作 |

### 4.2 权限原则

- 后端 API 必须执行权限校验，不能仅依赖前端隐藏按钮。
- 修改配置、发布、回滚、启动、停止、删除发布包等操作必须记录操作人、时间、目标及结果。
- SVN 密码不得以明文返回前端，不得出现在日志、异常信息或下载包清单中。

## 5. 核心业务流程

### 5.1 首次发布

1. 用户新增项目，填写项目名称、SVN 地址、凭据、工作目录及构建参数。
2. 平台执行连接测试，验证 SVN 地址、账号权限和目录结构。
3. 用户点击“发布”，平台创建唯一发布任务并对项目加锁。
4. 将源码检出至项目工作目录，记录实际 SVN 修订号。
5. 校验标准目录、项目文件、前端 `package.json` 等必要文件。
6. 确定本次构建专用的暂存 `publish` 目录；若目录已存在，先清空并确认目录中无残留文件。
7. 执行后台 `dotnet publish`，将结果重新生成到已清空的暂存 `publish` 目录。
8. 分别安装依赖并构建 admin、terminal 前端。
9. 将前端产物复制到后台发布结果的 `wwwroot/admin` 和 `wwwroot/terminal`。
10. 将平台保存的目标环境配置应用到发布目录的 `appsettings.json`。
11. 校验最终发布包，生成包含 SVN Revision 的 `release-info.json`、版本清单和文件摘要。
12. 将暂存目录提升为成功发布版本，并更新“当前版本”。
13. 用户可启动程序或下载 ZIP 包。

### 5.2 再次发布

- 平台重新获取 SVN 最新代码；也允许用户输入确定的 SVN 修订号进行可重复发布。
- 每次重新发布必须先清空本次任务的暂存 `publish` 目录，再执行后台发布和前端产物组装，确保旧文件不会混入新发布包。
- 清空操作只能针对安全数据根目录内、本次任务专属的暂存目录；不得清空当前运行版本或任一历史成功版本。
- 若程序正在运行，默认先完成新版本构建，构建成功后才停止旧进程并切换版本，缩短停机时间。
- 需求方确认允许版本切换期间出现短暂停机。
- 若新版本构建失败，旧程序继续运行，当前成功版本不得被覆盖。
- 系统必须提供历史成功版本回滚；若切换或启动失败，平台应允许一键回滚至最近一次成功且可用的版本。

### 5.3 运行控制

- 启动命令默认等价于：`dotnet iHR.Hosting.dll`。
- 启动工作目录必须为当前发布版本目录。
- 平台保存进程 ID、启动时间、命令、版本号、退出码和状态。
- 停止时先尝试优雅终止；超过配置的超时时间后，具有权限的用户可强制终止。
- 平台自身重启后，应重新识别受管进程；不得因为内存状态丢失而误判并重复启动。

## 6. 功能需求

### 6.1 登录与用户管理

| 编号 | 需求 |
|---|---|
| FR-AUTH-001 | 用户可使用用户名和密码登录、退出。 |
| FR-AUTH-002 | 系统初始化时创建管理员账户，并要求首次登录修改初始密码。 |
| FR-AUTH-003 | 密码必须使用安全哈希保存，不得明文保存。 |
| FR-AUTH-004 | 管理员可新增、禁用用户并分配角色。 |
| FR-AUTH-005 | 登录失败达到阈值后应短时锁定或限流。 |
| FR-AUTH-006 | 会话过期后需重新登录；所有变更接口需防止未授权调用。 |

### 6.2 项目管理

每个发布项目至少包含以下配置：

- 项目名称、唯一标识、说明、启用状态。
- SVN 仓库地址，例如 `https://120.79.42.59:8443/svn/IHR/IHR-国泰海通`。
- SVN 用户名、加密保存的密码、是否信任指定服务器证书。
- 本地工作根目录；系统根据项目标识生成隔离目录。
- 主干相对路径，默认 `05-trunk`。
- 后端项目目录，默认 `05-trunk/webapi/iHR.Hosting`。
- 后端项目文件，默认自动发现 `iHR.Hosting.csproj`。
- 后端入口程序集，默认 `iHR.Hosting.dll`。
- admin 前端目录，默认 `05-trunk/webui/admin`。
- terminal 前端目录，默认 `05-trunk/webui/terminal`。
- admin、terminal 前端包管理器统一使用 npm；构建命令默认 `npm run build`，可按项目调整。
- admin、terminal 构建输出目录，默认 `dist`。
- 构建配置，默认 `Release`；目标框架默认 `net8.0`。
- 运行环境变量、启动参数、停止超时和健康检查地址。
- 发布版本保留数量和发布包保留天数。

功能要求：

| 编号 | 需求 |
|---|---|
| FR-PROJ-001 | 管理员可新增、编辑、复制、禁用项目。 |
| FR-PROJ-002 | 项目保存前校验路径必须为相对路径，禁止目录穿越。 |
| FR-PROJ-003 | 提供“测试 SVN 连接”，显示脱敏后的失败原因。 |
| FR-PROJ-004 | 提供“检查构建环境”，检查 SVN、.NET SDK、Node.js 和包管理器版本。 |
| FR-PROJ-005 | 删除项目默认只做软删除；源码、发布历史和产物的物理删除需二次确认。 |

### 6.3 SVN 源码管理

| 编号 | 需求 |
|---|---|
| FR-SVN-001 | 首次发布可自动执行 SVN checkout。 |
| FR-SVN-002 | 后续发布可清理本地未版本化构建产物并执行 SVN update。 |
| FR-SVN-003 | 支持发布最新版本或指定有效修订号。 |
| FR-SVN-004 | 记录仓库地址、修订号、提交作者、提交时间及提交说明（SVN 可提供时）。 |
| FR-SVN-005 | 工作副本存在冲突、锁定、目录不完整或 URL 不一致时，发布终止并给出明确处理建议。 |
| FR-SVN-006 | 凭据仅在调用 SVN 时短暂使用，不写入命令日志；应避免将密码直接暴露在可被其他进程读取的命令行参数中。 |
| FR-SVN-007 | HTTPS 证书默认必须校验；若允许信任自签名证书，应由管理员显式配置并记录审计日志。 |

### 6.4 后端构建发布

| 编号 | 需求 |
|---|---|
| FR-BE-001 | 默认执行等价于 `dotnet publish <项目文件> -c Release -f net8.0 -o <暂存目录>` 的安全参数化命令。 |
| FR-BE-002 | 保存标准输出、标准错误、开始时间、结束时间、退出码和耗时。 |
| FR-BE-003 | 命令退出码非 0、超时或入口 DLL 不存在时，阶段判定失败。 |
| FR-BE-004 | 每次发布使用独立暂存/版本目录，不直接覆盖正在运行的目录。执行构建前必须清空本次任务的暂存 `publish` 目录并确认清理成功。 |
| FR-BE-005 | 支持管理员配置构建超时时间，超时后终止该任务启动的进程树。 |
| FR-BE-006 | 清空目录前必须校验其规范化绝对路径位于暂存根目录内，且不等于当前版本、运行版本或历史成功版本目录；校验失败时拒绝发布。 |

### 6.5 前端构建与产物组装

| 编号 | 需求 |
|---|---|
| FR-FE-001 | admin、terminal 分别独立安装依赖和执行生产构建。 |
| FR-FE-002 | admin、terminal 前端统一使用 npm，不需要自动识别 pnpm 或 yarn。 |
| FR-FE-003 | 存在有效 `package-lock.json` 时使用 `npm ci` 安装锁定依赖；不存在时使用 `npm install`，并在日志中提示依赖构建的可重复性风险。 |
| FR-FE-004 | admin 构建产物完整复制到 `publish/wwwroot/admin`。 |
| FR-FE-005 | terminal 构建产物完整复制到 `publish/wwwroot/terminal`。 |
| FR-FE-006 | 构建输出目录不存在、为空或构建命令失败时，整个发布失败。 |
| FR-FE-007 | 组装前清空本次暂存目录中的目标子目录，防止残留旧文件。 |
| FR-FE-008 | 前端构建命令和环境变量可按项目配置，但必须采用允许列表或参数化执行，禁止任意 Shell 拼接。 |

### 6.6 发布任务与版本管理

发布任务状态：`排队中`、`获取源码`、`构建后台`、`构建管理员前端`、`构建终端前端`、`组装产物`、`应用配置`、`校验`、`切换版本`、`启动中`、`成功`、`失败`、`已取消`、`已回滚`。

| 编号 | 需求 |
|---|---|
| FR-REL-001 | 用户点击发布后立即获得任务编号，页面可持续查看进度。 |
| FR-REL-002 | 同一项目同一时间只允许一个源码更新或发布任务。 |
| FR-REL-003 | 重复点击不得生成多个并发任务；按钮在任务执行期间禁用。 |
| FR-REL-004 | 不同项目可并行发布，并受系统配置的最大并发数限制。 |
| FR-REL-005 | 每个成功版本有唯一版本号，且版本号必须包含 SVN Revision，格式为 `yyyyMMddHHmmss-r<SVN修订号>`。 |
| FR-REL-006 | 保存版本目录、SVN 修订号、配置版本、构建耗时、触发人、结果及文件摘要。平台总览、版本历史和下载记录均显示 SVN Revision。 |
| FR-REL-007 | 用户可取消仍处于安全阶段的任务；进入版本切换阶段后不得任意取消。 |
| FR-REL-008 | 系统必须支持将一个历史成功版本设为当前版本并重新启动，形成完整回滚记录。 |
| FR-REL-009 | 清理过期版本时不得删除当前运行版本、当前版本或回滚所需的最近成功版本。 |
| FR-REL-010 | 最终发布目录根目录必须生成 UTF-8 JSON 文件 `release-info.json`，至少包含项目名称、发布版本、SVN 仓库地址、SVN Revision、构建时间、配置版本和入口程序集。该文件随程序一起打包。 |
| FR-REL-011 | 后台主程序集 `iHR.Hosting.dll` 的 `AssemblyInformationalVersion` 必须包含 `svn.r<SVN Revision>`；发布校验阶段读取程序集元数据验证，缺失或不一致时发布失败。 |

### 6.7 配置文件管理

建议将“环境配置”独立保存在平台中，而不是直接修改 SVN 工作副本；每次发布时覆盖到发布目录，使配置能够跨代码版本持续复用。

| 编号 | 需求 |
|---|---|
| FR-CFG-001 | 有权限用户可在页面查看和编辑目标项目的 `appsettings.json`。 |
| FR-CFG-002 | 保存前必须进行合法 JSON 语法校验，错误时指出行列位置且禁止保存。 |
| FR-CFG-003 | 每次保存生成配置版本，保留修改人、时间、说明和变更差异。 |
| FR-CFG-004 | 支持回退到历史配置版本。 |
| FR-CFG-005 | 发布时记录实际使用的配置版本；重新发布默认沿用当前启用配置。 |
| FR-CFG-006 | 修改运行版本配置前自动备份；用户选择后可触发重启使配置生效。 |
| FR-CFG-007 | 连接串、密码、密钥等敏感字段在只读页面默认脱敏；是否允许明文编辑由权限控制。 |
| FR-CFG-008 | 需求方确认下载包允许包含敏感配置。下载前必须显示风险提示，下载接口必须鉴权，下载行为必须写入审计日志。 |

### 6.8 程序启动、停止与健康检查

| 编号 | 需求 |
|---|---|
| FR-RUN-001 | 用户可启动当前成功版本，平台防止同一项目重复启动。 |
| FR-RUN-002 | 用户可停止、重启程序，并查看操作结果。 |
| FR-RUN-003 | 启动前验证入口 DLL、运行目录、配置文件及目标端口条件。 |
| FR-RUN-004 | 捕获应用标准输出和标准错误，按项目及日期保存运行日志。 |
| FR-RUN-005 | 页面显示未运行、启动中、运行中、停止中、已停止、异常退出、未知状态。 |
| FR-RUN-006 | 进程意外退出时记录退出码和最后日志；可配置是否自动重启及最大重试次数。 |
| FR-RUN-007 | 可配置 HTTP/HTTPS 健康检查 URL、超时、重试次数和期望状态码。 |
| FR-RUN-008 | 启动成功的判定可选择“进程存活”或“健康检查通过”；建议生产使用后者。 |
| FR-RUN-009 | 平台退出或重启不应主动终止已运行的业务程序。 |
| FR-RUN-010 | 首期不要求目标程序随 Windows 开机自动启动；平台不得擅自注册目标程序为 Windows 服务或开机启动项。 |

### 6.9 日志与审计

| 编号 | 需求 |
|---|---|
| FR-LOG-001 | 发布详情页实时展示各阶段日志，并支持自动滚动、暂停滚动和关键字检索。 |
| FR-LOG-002 | 支持查看及下载发布日志、运行日志，但需进行权限检查。 |
| FR-LOG-003 | 所有日志统一记录时间、级别、项目、任务/进程关联标识。 |
| FR-LOG-004 | 日志必须过滤 SVN 密码、令牌、连接串密码等已知敏感信息。 |
| FR-LOG-005 | 审计日志记录用户登录、项目变更、凭据变更、配置修改、发布、运行控制、回滚和下载。 |
| FR-LOG-006 | 日志按大小或日期滚动，并按配置的保留期限自动清理。 |

### 6.10 发布包压缩与下载

| 编号 | 需求 |
|---|---|
| FR-PKG-001 | 用户可选择成功发布版本生成 ZIP 文件。 |
| FR-PKG-002 | ZIP 保留发布目录的相对结构，不包含源码、SVN 元数据、临时文件和平台凭据。 |
| FR-PKG-003 | 压缩为异步任务，显示进度、文件大小、生成时间和失败原因。 |
| FR-PKG-004 | 下载使用鉴权接口，不能通过猜测静态 URL 绕过权限。 |
| FR-PKG-005 | 生成 SHA-256 摘要供下载后校验。 |
| FR-PKG-006 | 同一版本的相同内容可复用已有压缩包；版本或配置变化后应重新生成。 |
| FR-PKG-007 | ZIP 文件名必须包含 SVN Revision，格式建议为 `<项目标识>-yyyyMMddHHmmss-r<Revision>.zip`。 |
| FR-PKG-008 | ZIP 根目录必须包含 `release-info.json`；其中的 SVN Revision、ZIP 文件名中的 Revision、平台发布记录及主程序集版本信息必须完全一致。 |

`release-info.json` 示例：

```json
{
  "projectName": "IHR-国泰海通",
  "releaseVersion": "20260720163000-r12345",
  "svnUrl": "https://example/svn/IHR/IHR-国泰海通",
  "svnRevision": 12345,
  "builtAtUtc": "2026-07-20T08:30:00Z",
  "configurationVersion": 7,
  "entryAssembly": "iHR.Hosting.dll"
}
```

版本信息文件不得包含 SVN 用户名、密码、令牌或其他平台凭据。

### 6.11 首页与页面要求

页面采用简洁、清晰、响应式的后台管理风格，优先桌面端，兼容常见笔记本分辨率。

| 页面 | 主要内容 |
|---|---|
| 登录页 | 用户名、密码、登录错误提示 |
| 项目总览 | 项目名称、SVN 修订号、当前版本、运行状态、最近发布、快捷操作 |
| 项目详情 | 基本配置、环境检查、SVN 测试、路径和构建设置 |
| 发布中心 | 发布按钮、修订号选项、阶段进度、实时日志、取消操作 |
| 版本历史 | 版本、修订号、配置版本、触发人、时间、结果、下载、回滚 |
| 运行管理 | 启动、停止、重启、PID、启动时间、健康状态、运行日志 |
| 配置中心 | JSON 编辑器、语法校验、差异比较、版本历史、恢复和重启提示 |
| 用户管理 | 用户、角色、启用状态、重置密码 |
| 系统设置 | 工具路径、并发数、目录、超时、保留策略、日志策略 |
| 审计日志 | 操作人、操作、目标、时间、IP、结果及筛选 |

交互要求：

- 危险操作使用明确的二次确认，显示项目名和目标版本。
- 长任务不得阻塞 HTTP 请求；页面刷新或重新登录后仍可恢复任务状态。
- 错误提示应包含失败阶段和可行动建议，不直接展示堆栈或敏感参数。
- 状态不能只依赖颜色表达，须同时提供文字或图标。

## 7. 业务规则

1. 一个项目只能有一个当前版本和至多一个受管运行实例。
2. 只有全部构建及校验阶段成功的产物才能成为成功版本。
3. 失败或取消的暂存产物不得覆盖当前版本，可按策略保留用于排障。
4. 发布时记录确定的 SVN 修订号；页面中的“最新”最终必须解析为具体修订号。
5. 配置与代码版本分别管理，一个发布记录同时引用两者。
6. 发布、回滚或运行控制发生冲突时，以项目级互斥锁串行处理。
7. 平台不得允许用户配置任意绝对输出目录；所有源码、版本和包目录必须位于管理员设置的安全根目录内。
8. 平台应使用进程 API 的参数列表调用外部工具，不拼接未经校验的 Shell 命令。
9. Windows 下路径比较不区分大小写；Linux 下必须按大小写处理，项目相对路径配置须保持真实大小写。
10. SQLite 数据库、加密密钥、日志、源码工作区和发布产物应位于彼此独立的目录。

## 8. 建议技术边界与组件

### 8.1 管理平台后端

- ASP.NET Core .NET 8 Web API。
- EF Core + SQLite，启用迁移管理和并发控制。
- 后台任务采用 ASP.NET Core Hosted Service + 持久化任务表；如后续任务量增大，可替换为专用任务框架。
- 实时日志可采用 SignalR；断线重连后通过任务日志接口补齐缺失内容。
- 外部命令封装为 `ISourceControlService`、`IBackendBuilder`、`IFrontendBuilder`、`IProcessManager`、`IArchiveService` 等接口。
- 操作系统差异封装在 Windows/Linux 实现中，不在业务层直接调用特定平台命令。

### 8.2 管理平台前端

- Vue 3 + TypeScript + Vite。
- 路由、状态管理、HTTP 请求和组件库采用主流稳定方案，具体库在设计阶段确定。
- JSON 编辑器支持格式化、行号、语法错误定位和差异查看。

### 8.3 建议目录布局

```text
data-root/
├─ database/          # SQLite 数据库
├─ keys/              # 数据保护密钥，严格限制权限
├─ workspaces/        # SVN 工作副本
│  └─ <project-id>/
├─ releases/          # 不可变的成功版本
│  └─ <project-id>/<version>/
├─ staging/           # 构建暂存目录
├─ packages/          # ZIP 发布包
└─ logs/              # 平台、发布及运行日志
```

## 9. 数据需求

建议的核心实体：

| 实体 | 关键内容 |
|---|---|
| User / Role | 用户、密码哈希、角色、状态、最后登录 |
| Project | 项目、SVN 地址、目录、构建配置、运行配置 |
| Credential | 加密凭据、类型、更新时间、最后验证结果 |
| DeploymentTask | 发布任务、阶段、进度、触发人、起止时间、错误 |
| DeploymentStageLog | 任务阶段、日志序号、时间、级别、内容 |
| Release | 版本号、SVN Revision、目录、配置版本、摘要、状态、版本信息文件 |
| AppConfiguration | JSON 内容/加密内容、版本、修改人、说明 |
| ManagedProcess | 项目、版本、PID、启动时间、状态、退出码 |
| Package | 版本、路径、大小、SHA-256、生成状态 |
| AuditLog | 用户、IP、操作、目标、结果、时间、详情摘要 |

数据完整性要求：

- 删除项目不级联物理删除发布与审计历史。
- 任务状态变更、版本切换等关键操作使用事务。
- SQLite 定期备份；备份文件不得包含明文 SVN 密码。
- 时间在数据库中统一保存为 UTC，页面按用户或系统时区显示。

## 10. 非功能需求

### 10.1 安全性

- 全站应使用 HTTPS；生产环境不允许明文 HTTP 传输凭据。
- SVN 密码和敏感配置使用由操作系统保护或外置密钥保护的加密机制保存；仅“可逆加密但密钥同库保存”不合格。
- 防止命令注入、路径穿越、Zip Slip、越权下载和日志注入。
- 上传/编辑内容、命令参数、路径、修订号均需服务端校验。
- API 返回统一错误编号，生产环境不暴露堆栈。
- 依赖和工具版本在系统设置页可查看，便于安全升级。

### 10.2 可靠性

- 发布任务状态和日志必须持久化，浏览器关闭不影响任务执行。
- 平台异常重启后，未完成任务应标记为“中断待处理”或按安全策略恢复，不得直接视为成功。
- 构建使用暂存目录，只有通过校验的版本才允许切换。
- 默认至少保留最近 3 个成功版本，数量可配置。
- 对 SQLite、加密密钥和配置提供备份与恢复说明。

### 10.3 性能与容量

- 除日志流外，普通 API 在本机正常负载下 95% 响应时间目标小于 1 秒。
- 总览页在 100 个项目、每项目 1,000 条发布记录规模下可分页正常使用。
- 日志和历史列表必须分页或增量加载，禁止一次加载完整大文件。
- ZIP 生成采用流式 I/O，避免将整个发布包加载到内存。
- 最大并发发布数默认 2，可按机器 CPU、内存配置调整。

### 10.4 可维护性

- 后端分层清晰，外部工具调用、任务编排、数据访问和 API 分离。
- 数据库结构变更通过 EF Core Migration 管理。
- 关键流程提供自动化单元测试和集成测试。
- 日志具备关联 ID，可从一次发布追踪到各阶段与外部进程。

### 10.5 兼容性

- 首期支持 Windows 10/11 或 Windows Server 2019 及以上的受支持版本，具体基线待确认。
- 浏览器支持当前主流 Chrome、Edge。
- 后续 Linux 目标为主流 x64 发行版；路径、进程、权限、信号和工具发现不得写死为 Windows 实现。
- 发布目标应用必须由目标机器预装匹配的 .NET 8 Runtime，或后续扩展为 self-contained 发布。

## 11. 异常处理

| 场景 | 系统行为 |
|---|---|
| SVN 无法连接/认证失败 | 任务失败，保留旧版本运行；显示连接或权限类提示，隐藏密码 |
| SVN 工作副本冲突 | 停止发布，不自动丢弃用户修改；提示管理员处理或重新检出 |
| .NET/Node/包管理器缺失 | 环境检查或相应构建阶段失败，显示缺失工具及要求版本 |
| 前端依赖下载失败 | 任务失败，保留完整阶段日志，可重新发布 |
| 后端或前端编译失败 | 任务失败，不切换当前版本，不停止旧进程 |
| 磁盘空间不足 | 发布前预检并拒绝任务；执行中发生时安全失败并清理可识别的暂存文件 |
| 配置 JSON 无效 | 禁止保存或禁止进入切换阶段 |
| 目标端口被占用 | 启动失败并显示占用情况；不得随意终止非受管进程 |
| 启动后健康检查失败 | 标记启动失败，可查看日志并选择回滚 |
| 平台重启 | 恢复任务、版本和受管进程状态，识别未知/中断状态 |
| 下载中包被清理 | 活跃下载期间加引用保护，下载结束后再按策略清理 |

## 12. 验收标准

### 12.1 主流程验收

1. 配置有效 SVN 地址和凭据后，连接测试成功，且页面及日志中不出现明文密码。
2. 对符合目录规范的项目执行首次发布，能生成包含入口 DLL、`wwwroot/admin` 和 `wwwroot/terminal` 的完整版本。
3. 发布记录显示实际 SVN 修订号、代码版本、配置版本、触发人、各阶段耗时及结果。
4. 点击启动后，系统以发布目录为工作目录运行 `dotnet iHR.Hosting.dll`，页面显示 PID 和健康状态。
5. 修改 SVN 代码后再次发布，系统获取新修订号并生成独立新版本；新版本成功前旧程序保持运行。
6. 在暂存 `publish` 目录预先放入一个旧文件后再次发布，该目录会先被完整清空，最终版本中不存在该旧文件；当前运行版本和历史成功版本未被清空。
7. 后端或任一前端故意制造编译错误时，发布失败且当前运行版本不受影响。
8. 在线修改 `appsettings.json` 时，非法 JSON 不能保存；合法配置能版本化并用于下次发布。
9. 对成功版本生成 ZIP 后可鉴权下载，解压结构正确、包含已应用的目标环境配置，SHA-256 校验一致。
10. ZIP 文件名、包内 `release-info.json`、平台发布记录和 `iHR.Hosting.dll` 的信息版本均包含同一个 SVN Revision；任一处缺失或不一致时不得将该版本标记为发布成功。
11. 启动失败时可查看运行日志；选择历史成功版本回滚后能够重新运行。
12. 平台重启后，仍能查看历史任务，并能正确识别此前启动的受管程序；Windows 重启后不要求自动启动目标程序。

### 12.2 安全与边界验收

1. 只读用户不能通过直接调用 API 发布、修改配置、控制进程或下载受限包。
2. 路径中输入 `..`、绝对路径或工作根目录外路径时，服务端拒绝。
3. 构建参数中输入 Shell 控制字符不能导致额外命令执行。
4. SVN 密码不出现在数据库明文字段、应用日志、发布日志、审计日志和浏览器响应中。
5. 同一项目快速连续点击发布，只产生一个有效执行任务。
6. 清理历史版本不会删除当前运行版本及策略要求保留的回滚版本。

## 13. 部署与运行前置条件

Windows 主机需预装并可由管理平台运行账户访问：

- .NET 8 SDK（用于构建管理目标项目）。
- .NET 8 ASP.NET Core Runtime（用于运行目标项目；SDK 已包含相应运行能力时仍建议明确校验）。
- SVN 命令行客户端。
- 与目标前端项目匹配的 Node.js 版本。
- npm 包管理器；其版本应与两个前端项目兼容。
- 足够的源码、依赖缓存、发布版本、日志和压缩包磁盘空间。

管理平台运行账户应采用最小权限，只能访问配置的数据根目录和执行所需工具，不建议使用本机管理员账户长期运行。

## 14. Linux 迁移预留要求

1. 全部路径使用 .NET 路径 API 组合，不硬编码反斜杠、盘符或大小写假设。
2. 进程管理定义统一接口：Windows 可使用进程树/Job Object 等实现，Linux 可使用进程组、信号或 systemd 适配器。
3. 凭据保护定义统一接口：Windows 可对接 DPAPI/Data Protection，Linux 可对接密钥文件、环境密钥或外部 Secret Store。
4. 工具路径、数据根目录、Shell/可执行文件名均可配置或自动发现。
5. 不依赖 IIS；管理平台和目标应用均可用 Kestrel 独立运行，反向代理配置在本期范围外。
6. 文件权限、可执行权限、符号链接和文件名大小写差异纳入跨平台集成测试。

## 15. 建议迭代计划

| 阶段 | 交付内容 |
|---|---|
| M1 基础框架 | 登录、角色、项目配置、SQLite、系统环境检查 |
| M2 发布流水线 | SVN、后台构建、双前端构建、产物组装、实时日志 |
| M3 运行与配置 | 配置版本、启动/停止/重启、健康检查、运行日志 |
| M4 版本治理 | 历史版本、回滚、ZIP 下载、摘要、保留策略、审计 |
| M5 加固验收 | 异常恢复、安全测试、备份恢复、Windows 部署文档、Linux 适配验证 |

## 16. 已确认事项与待确认事项

### 16.1 第一轮已确认事项

| 编号 | 确认结论 |
|---|---|
| C-001 | 需要多用户角色权限，按系统管理员、发布操作员、只读用户实施。 |
| C-002 | 发布新版本时允许在停止旧版本、切换并启动新版本期间短暂停机。 |
| C-003 | `appsettings.json` 由发布管理平台维护，默认不自动提交回 SVN。 |
| C-004 | 下载的发布 ZIP 允许包含敏感配置，但必须鉴权、风险提示并记录审计。 |
| C-005 | 目标程序不需要随 Windows 开机自动启动。 |
| C-006 | 首期必须提供历史成功版本回滚功能。 |
| C-007 | 再次发布前必须清空本次任务的暂存 `publish` 目录，再重新生成完整发布内容。 |
| C-008 | admin、terminal 前端统一使用 npm 管理和安装依赖。 |
| C-009 | 打包后的程序版本信息必须包含 SVN Revision，并可从包名、版本信息文件和主程序集信息版本中核对。 |

### 16.2 仍待确认事项

以下问题仍会影响详细设计或验收，请在开发前确认：

1. 首期需要同时管理多少个项目、环境和运行实例？本文暂按一个项目对应一个环境、一个运行实例设计。
2. SVN 使用的具体客户端及版本是什么？服务器是否使用自签名 HTTPS 证书？
3. admin、terminal 的包管理器已确认为 npm；仍需核实各自的构建命令和输出目录。本文暂按 `npm run build` 和 `dist` 设计。
4. 两个 Vue 应用是否已正确配置 `/admin/`、`/terminal/` 基础路径和前端路由刷新回退？该项直接影响发布后访问。
5. 后端 `.csproj` 的准确文件名、目标框架和发布参数是否固定？是否需要 self-contained、单文件或指定 Runtime Identifier？
6. 目标应用监听的端口、启动参数、环境变量，以及可用于健康检查的 URL 是什么？
7. 版本回滚采用用户手动点击回滚，还是健康检查失败后系统自动回滚？本文暂按“人工一键回滚”设计。
8. 成功版本、失败构建、日志和 ZIP 分别保留多久或多少份？本文建议至少保留最近 3 个成功版本。
9. Linux 是“后续可能支持”还是首版即要求同一套代码可通过测试运行？两者会影响首期测试范围。

## 17. 需求确认

需求方确认时建议逐项标记：

- `[确认]`：按文档实现。
- `[修改]`：说明期望行为。
- `[延期]`：移至后续版本。
- `[删除]`：不再需要。

本文件经确认并形成版本基线后，新增或变更需求应记录变更原因、影响范围、优先级和计划版本。
